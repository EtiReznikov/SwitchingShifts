<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.switchingshifts">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".RemoveWorker" />
        <activity android:name=".MangerScreen" />
        <activity android:name=".Settings" />
        <activity android:name=".Shifts" />
        <activity android:name=".DeleteShift" />
        <activity android:name=".AddShift" />
        <activity android:name=".Messages" />
        <activity android:name=".MyShifts" />
        <activity android:name=".PersonalDetails" />
        <activity android:name=".WorkerScreen" />
        <activity android:name=".ChangePass" />
        <activity android:name=".Login" />
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>